package:
  ngnix:
    title: Ngnix is installed
    installed: true

file:
  /var/www/static_web_app/index.html:
    title: Static site home page
    exists: true
    owner: root
    group: root
    contains: ["Hello World!"]
  /etc/nginx/sites-enabled/default:
    title: Apache2 configuration
    exists: true
    owner: root
    group: root
    contains: ["return 301 "]
